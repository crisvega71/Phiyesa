@model IEnumerable<Phiyesa.PartCategory>

@{
    ViewBag.Title = "Auto Part Categories";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<div class="div_admin_pageheader">
    <table class="table_admin_pageheader">
        <tr>
            <td><h3>Auto Part Categories</h3></td>
            <td align="right">
                @{
                    //if (Session["USER_TYPE"].ToString() == "1" || Session["USER_TYPE"].ToString() == "2")
                    //{
                    <a href=@Url.Action("Create", "PartCategories")><img src="~/images/add_icon.png" /></a>
                    //}
                }
            </td>
        </tr>
    </table>
</div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartCategoryDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartCategoryDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id })
        </td>
    </tr>
}

</table>
